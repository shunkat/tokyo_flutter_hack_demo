import 'package:flutter/material.dart';

class FirebasePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Firebase Page')),
      body: Center(
        child: Text('Firebase画面です。'),
      ),
    );
  }
}